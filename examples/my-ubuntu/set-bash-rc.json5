{
  vars: {
    bashRC: {
      local: true,
      value: "${fs.readFile(path.join(sys.SCRIPT_DIR,'/templates/bash-rc.sh'))}",
    },
  },
  assertions: [
    {
      assert: "FileContains",
      with: {
        path: "${env.HOME}/.bashrc",
        contents: "${bashRC}",
      },
    },
  ],
}
