{
  vars: {
    local: {
      bashRC: "{fs.readFile(path.join(sys.SCRIPT_DIR,'my-ubuntu/templates/bash-rc.sh'))}",
    },
  },
  assertions: [
    {
      assert: "FileContains",
      with: {
        path: "{env.HOME + '.bashrc'}",
        contents: "{bashRC}",
      },
    },
  ],
}
