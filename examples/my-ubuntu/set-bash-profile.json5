{
  vars: {
    bashProfile: {
      local: true,
      value: "${fs.readFile(path.join(sys.SCRIPT_DIR,'my-ubuntu/templates/bash-profile.sh'))}",
    },
  },
  assertions: [
    {
      assert: "FileContains",
      with: {
        path: "${env.HOME}/.bash_profile",
        contents: "${bashProfile}",
      },
    },
  ],
}
